{
  "name": "punkcubes",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "check": "npm run test && npm run build"
  },
  "dependencies": {
    "three": "^0.185.1",
    "typescript": "^5.8.3"
  },
  "devDependencies": {
    "@types/three": "^0.185.0",
    "vite": "^7.0.0",
    "vitest": "^5.0.0"
  }
}
